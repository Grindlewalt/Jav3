# voicebox — STT/TTS inference sidecar. Runs on an x86 box (the main server),
# NEVER on the Pi. All three engines are CPU + ONNX/CTranslate2 — no torch.
fastapi>=0.115
uvicorn[standard]>=0.30       # also provides websockets for demo_client.py
faster-whisper>=1.0.2         # STT: Whisper via CTranslate2. >=1.0.2 for the
                              # `hotwords` arg the Pi's vocabulary push uses;
                              # large-v3-turbo needs a build that knows the
                              # name (1.2.x does). Default model is
                              # large-v3-turbo on the GPU — see docs/voice.md
                              # for the noise measurements that chose it, and
                              # VOICEBOX_WHISPER=small to roll back.
# TTS is NOT installed here. The architect voice runs as its own service
# (architect_tts.py) in a separate py3.12 venv, because it needs torch and this
# one is deliberately torch-free; tts.py talks to it over HTTP with nothing but
# stdlib. See scripts/architect-tts.service. Kokoro was the engine until
# 2026-08-03 and is gone — it cost ~550 ms fixed, which was most of the old
# latency budget.
pysilero-vad>=2.0             # VAD: silero v5, onnxruntime-only, model bundled
numpy>=1.26
pytest>=8.0                   # for tests/ (self-skips without models)
# Wake word ("hey jarvis", on by default when importable): openwakeword 0.6
# pins tflite-runtime, which has no py3.14 wheels — the onnx path never
# touches it, so install without deps:
#   pip install --no-deps "openwakeword==0.6.0" && pip install scipy tqdm requests
# GPU whisper (VOICEBOX_DEVICE=cuda) additionally needs:
#   pip install nvidia-cudnn-cu12 nvidia-cublas-cu12
# and LD_LIBRARY_PATH pointing at those wheels' lib dirs (see README).
# Whisper gets the whole card: the TTS is a separate process and, on the
# current layout, a separate GPU (see docs/voice.md).
